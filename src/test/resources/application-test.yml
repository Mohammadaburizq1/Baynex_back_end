spring:
  datasource:
    url: jdbc:postgresql://localhost:1/test_initializer_required
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  media:
    storage-dir: ${java.io.tmpdir}/shoplink-test-media
    public-base-url: http://localhost:8081
  jwt:
    secret: test-secret-key-at-least-32-characters-long!!
  security:
    login:
      mfa-dev-bypass: true
