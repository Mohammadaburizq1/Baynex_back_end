package com.byonix.shoplink.domain.enums;

/** PENDING: created, waiting for its activation code. ACTIVE: holds a device credential. REVOKED: terminal. */
public enum PosDeviceStatus {
    PENDING,
    ACTIVE,
    REVOKED
}
